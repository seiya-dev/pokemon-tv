{
    "installCommand": "pnpm install",
    "buildCommand": "node ./api/build.mjs",
    "outputDirectory": "watch",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/host.mjs"
        }
    ]
}
