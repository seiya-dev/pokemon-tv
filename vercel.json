{
    "installCommand": "pnpm install",
    "buildCommand": "node ./app.js",
    "outputDirectory": "watch",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/app.mjs"
        }
    ]
}
