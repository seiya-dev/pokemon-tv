{
    "installCommand": "pnpm install",
    "buildCommand": "node ./app/build.mjs",
    "outputDirectory": "web",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/host.mjs"
        }
    ]
}
